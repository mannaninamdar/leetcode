const arr1 = [1, 2, 3, 4, 5]
const arr2 = [2, 3, 1, 5]

/*
// add two arrays using reduce
const arr1Sum = arr1.reduce((a, b) => a + b, 0);
const arr2Sum = arr2.reduce((a, b) => a + b, 0);
const totalSum = arr1Sum + arr2Sum;
console.log(`Total Sum : ${totalSum}`);
*/

/*
// convert arrays to objects
const object1 = {} as any;
arr1.forEach((num) => {
    object1[num] = num
})

console.log(`Object 1: ${JSON.stringify(object1)}`);

// push arr2 elements to arr1 
const arr3 = [...arr1, ...arr2];
console.log(`Merged Array: [${arr3}]`);

// unique values from arr3
const values = [...new Set(arr3)];
console.log(`Unique Values: [${values}]`);

// push values to arr2 using .push method 
const emptyArr: number[] = [];
values.forEach((numb) => {
    console.log(`Pushing number: ${numb}`);
    emptyArr.push(numb)
})
*/




